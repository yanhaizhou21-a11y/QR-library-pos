"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.activeTabWidth = exports.activeTabTop = exports.activeTabRight = exports.activeTabLeft = exports.activeTabHeight = exports.activeTabBottom = void 0;
/**
 * Indicates the distance on the left side from the parent's container if the tab is active.
 * @type {number}
 */
const activeTabLeft = exports.activeTabLeft = '--active-tab-left';
/**
 * Indicates the distance on the right side from the parent's container if the tab is active.
 * @type {number}
 */
const activeTabRight = exports.activeTabRight = '--active-tab-right';
/**
 * Indicates the distance on the top side from the parent's container if the tab is active.
 * @type {number}
 */
const activeTabTop = exports.activeTabTop = '--active-tab-top';
/**
 * Indicates the distance on the bottom side from the parent's container if the tab is active.
 * @type {number}
 */
const activeTabBottom = exports.activeTabBottom = '--active-tab-bottom';
/**
 * Indicates the width of the tab if it is active.
 * @type {number}
 */
const activeTabWidth = exports.activeTabWidth = '--active-tab-width';
/**
 * Indicates the height of the tab if it is active.
 * @type {number}
 */
const activeTabHeight = exports.activeTabHeight = '--active-tab-height';